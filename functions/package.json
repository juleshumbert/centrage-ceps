{
  "name": "centrage-functions",
  "description": "Cloud Function centrage.ceps09.com : /api/placement, execute le solveur C++ placement (HiGHS) sur un stick",
  "private": true,
  "main": "index.js",
  "engines": { "node": "22" },
  "scripts": {
    "test": "node --test",
    "deploy": "firebase deploy --only functions:centrage --project paraclub-planning-f966c"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0"
  }
}
